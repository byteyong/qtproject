name: bible_qt_app
description: 성경 QT 묵상 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  google_fonts: ^6.2.1
  google_generative_ai: ^0.4.5
  shared_preferences: ^2.2.0 # 로그인 상태를 앱이 꺼져도 기억할 수 있도록

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/bible/bible.txt
